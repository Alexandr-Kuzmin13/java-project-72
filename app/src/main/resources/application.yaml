# datasource:
#   default: ${APP_ENV}
#   development:
#     username: ""
#     password: ""
#     url: "jdbc:h2:~/test"
#   production:
#     username: ${PGUSER}
#     password: ${PGPASSWORD}
#     url: jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}

# ebean:
#   development:
#     migration:
#       run: true
#   production:
#     migration:
#       run: true
      
datasource:
  default: ${APP_ENV}
  development:
    username: sa
    password: sa
    url: jdbc:h2:mem:dev_db
  production:
    username: ${PGUSER}
    password: ${PGPASSWORD}
    url: jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}

ebean:
  migration:
    run: true
